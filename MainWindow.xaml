<Window x:Class="RoomRentalAppilcationForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rental Room Application" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Main Label -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Rental Room Application" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Name Section -->
        <Label Grid.Row="1" Grid.Column="0" Content="Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBox Width="150" Margin="0,0,10,0" Height="25" Text="First Name" Foreground="Gray"
                     GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
            <TextBox Width="150" Height="25" Text="Second Name" Foreground="Gray"
                     GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
        </StackPanel>

        <!-- Phone Number Section -->
        <Label Grid.Row="2" Grid.Column="0" Content="Phone Number:" VerticalAlignment="Center" Margin="0,10,10,0"/>
        <TextBox Grid.Row="2" Width="300" Height="25" Margin="103,10,377,1" Text="Phone Number" Foreground="Gray"
                 GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" Grid.ColumnSpan="2"/>

        <!-- Address Section -->
        <Label Grid.Row="3" Grid.Column="0" Content="Address:" VerticalAlignment="Center" Margin="0,10,10,0"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Left">
            <TextBox Width="300" Margin="0,10,0,0" Height="25" Text="City" Foreground="Gray"
                     GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
            <TextBox Width="300" Margin="0,10,0,0" Height="25" Text="Postal Code" Foreground="Gray"
                     GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
        </StackPanel>
        <Label x:Name="Address" Content="Address:" HorizontalAlignment="Center" Margin="0,10,0,0" Grid.Row="3" VerticalAlignment="Top" RenderTransformOrigin="-6.162,-3.256" Width="103" Height="59"/>
    </Grid>
</Window>
